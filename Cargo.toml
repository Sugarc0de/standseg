[package]
name = "fast_segment"
version = "0.1.0"
edition = "2021"
rust-version = "1.90"

[dependencies]
clap = { version = "4", features = ["derive"] }
smallvec = "1"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = 1
