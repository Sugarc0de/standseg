[package]
name = "standseg"
version = "0.1.0"
edition = "2021"
rust-version = "1.90"
license = "MIT"
description = "Multi-resolution region-growing image segmentation (Ye et al. 2025; Woodcock & Harward 1992)"
repository = "https://github.com/Sugarc0de/standseg"

[dependencies]
clap = { version = "4", features = ["derive"] }
png = "0.18.1"
rayon = "1.12.0"
smallvec = "1"
# "zstd" is not on by default; cloud-optimized GeoTIFFs increasingly use it.
tiff = { version = "0.11.3", features = ["zstd"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = 1

[dev-dependencies]
png = "0.18.1"
tiff = { version = "0.11.3", features = ["zstd"] }
# The tiff encoder has no ZSTD writer, so the ZSTD reader test compresses its
# own strip. Same crate the decoder pulls in, so this adds nothing to the tree.
zstd = "0.13"
